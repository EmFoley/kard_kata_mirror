Here Are All Your Decks:
<% @decks.each do |deck| %>
<div class="deck">
  <%= deck.name %>
  <button class="view-deck-button" type="button"><a href="/decks/<%= deck.id %>">View</a></button>
  <button class="edit-deck-button"><a href="/decks/<%= deck.id %>/edit">Edit</a></button>
  <form class="deck-delete-form" method="post" action="/decks/<%= deck.id %>">
    <input type="hidden" name="_method" value="delete">
    <input class="delete-deck-button" type="submit" value="Delete">
  </form>
</div>
<% end %>